# XORSAT Solvers

XORSAT is intentionally deferred until the current fixture and parser contract
is made explicit. The old XORSAT files use a different coupling/field format
than the canonical pairwise fixtures used by the first SK and EA baselines.

