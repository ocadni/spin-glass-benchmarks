# Test Data

This directory stores development-time reference data for tests.

`instances/` is a snapshot of the legacy `benchmarks/` tree. Tests can compare
generated or materialized repository-root `instances/` files against this
reference without depending directly on the active `benchmarks/` directory.
