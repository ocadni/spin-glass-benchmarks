# Test Data

This directory stores development-time reference data for tests.

`instances/` is a snapshot of the legacy `benchmarks/` tree. Parser and
regression tests can use this reference data without depending on an active
`benchmarks/` directory.
